#include <bits/stdc++.h>

using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	
	int t; cin >> t;
	while(t--) {
		int n; cin >> n;
		vector <int> v;
		while(n--) {
			int aux; cin >> aux;
			v.push_back(aux);
		}
		multiset <int> m(v.begin(), v.end());
		
		int i=1, j=n;
		while(!m.empty()) {
			if()
		}
			
	}
	
	return 0;
}
